# RoomLab v13.1·主题密室

日期：2026-09-09。本批针对 QA 发现的资源加载、断线与无效界面刷新问题。

## 改动

- 17 张密室封面增加 WebP 编码副本，总体积从 4,563,305 字节降至 564,772 字节，减少 87.62%。原 PNG 保留为回退，图片尺寸不变。
- 首张封面优先加载，相邻封面有界预加载并预解码；快速翻页不会让旧请求覆盖新选择。封面、名称与实际建房密室保持一致；两种格式均加载失败时显示占位，仍能创建或加入房间。
- 独立情报空投增加连接存活检测：短时波动暂停操作和计时，持续失联停止本局并提供重建入口；重建保留显式信令配置，清理旧会话监听和输入。
- 密室对局硬断线提示、集合/进入守卫、恢复与重建；修复空投退出后的监听残留。未知玩法参数回到主页；地图节点保持扁平按钮样式。
- 机关盒、影子拼图、证物还原、镜面引光在渲染状态未变化时跳过无效 DOM 重建。主题压力机关复用静态 SVG，仅更新动态属性，玩法时序、输入租期和广播保持不变。
- 可见版本和相关 JS/CSS、封面缓存版本统一为 13.1。

## 验证

在隔离发布目录执行静态、19 玩法入口冒烟、三种手机视口视觉、真实本机双端玩法和性能五套组合检查，66/66 通过。另完成封面弱网/回退/快速切换/真实建房专项及独立空投断线、恢复、重建、监听清理专项。

封面链路受控测试使用 180ms 延迟、96KiB/s、全新浏览器上下文且禁用缓存：首张 WebP 约 451ms，预解码相邻切换约 44ms，纯 PNG 冷解码约 2683ms。该结果只衡量封面链路，不是整页弱网或实体手机测试。

本机联机实测选择 host↔host UDP；无法根据公共信令响应时间断言游戏数据走中继，也未以更换服务器作为本批修复。实体手机跨运营商、跨地域延迟仍需实机验证。

## 范围

此次发布不包含仍在制作的手绘大厅、建筑地图、人物动作素材，也不包含本地另批档案教学改动。采用已发布基线叠加明确 QA 文件清单，保留其他文件。
