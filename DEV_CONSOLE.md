# 开发者验收台

状态：本地实现与验收，不是发布记录。APP_VER 和资源版本均未修改。独立工具布局已由用户在原总控确认。

启动既有本地服务后打开 `http://127.0.0.1:8080/dev-console.html`。需要页面和游戏同源，并使用现有 Peer 服务。可继承 URL 中的 peer 配置查询参数。不能用 file:// 打开。

## 使用

选择密室、玩法和难度，点击“一键开局”。选择密室会先选中该密室主玩法，也可另选玩法进行专项验证。验收台会创建两个真正的 390×844 游戏 iframe，完成 A 建房、B 加入、密室资源屏障、必要的教学练习与双端准备。进入“进行中”后默认手动操作两端，也可选择首批已支持的单端同伴托管。点击某端后，键盘输入由该游戏页面接收。

“重试本关”按上一局实际配置新建一轮；如果已改动下拉，会把下拉恢复为重试配置。“一键开局”使用当前下拉。“返回选择”停止当前轮次，清理托管与连接并保留筛选，加载或准备期间也能取消。首次目录读取被取消后，可用“重新读取目录”恢复。

档案窃取开局停在可操作的阅读页，由 A 手动开始扫描。默数开灯在双端手动模式不替 A 计时或按开关；托管 A 时按真实 10.5 秒计时控制自己的开关。教学练习中的点击/按住由工具执行，正式解谜只操作指定托管端，另一端继续由玩家操作。

## 能力边界

目录来自运行中 PLAYGROUNDS 与 RoomProgression.supportsDungeon，当前 19 个双人玩法、17 间密室。不会读取 record.js 的旧枚举，不引入录像画布。单端托管仅开放下列已验证能力。

| 玩法 | 可选档位实际影响 |
| --- | --- |
| 密码推条 | 入门 6 条固定 1234；进阶 8 条随机密码 |
| 机关盒、影子拼图、证物还原、镜面引光 | 1/2/3 层（案、组），并沿用各自机关参数 |
| 档案窃取 | 4 位数字、7 位数字、6 位数字字母混合、3 题限时算术 |
| 默数开灯 | 4 档节奏容错及搜索布局/道具要求 |
| 警报拆线、动力稳压 | 现有 4 档规则参数与限时 |
| 最终对峙 | 仅支持标准档，不虚设高难度 |
| 其余玩法 | 标准及三档限时收紧；不宣称新增谜题结构 |

默认“换题”重新创建游戏实例与随机开局。固定地图、案件题库和基础结构可能重复，入门密码数字始终为 1234。“同题”选项禁用：现有玩法缺乏统一固定随机种子重试接口。

本工具为单关入口验收，会在新建地图上配置一个节点的 mode、type、roomTutorial、roomAdvanced、routeTier、coopConfig，双方各写入相同的入口配置，再调用既有 dgBroadcast/dgEnterHost。boss 使用现有 boss 节点，其余使用第一层事件节点。这不会模拟已通关楼层或修改 done/hp/history/win。任意密室与玩法组合仅用于专项测试；正常游玩仍遵守正式地图路线和解锁规则。

## 隔离与清理

每轮有独立 AbortController、递增 sessionId 和新 iframe。重试、返回、失败或关闭页面会取消父页等待/监听/监测及托管计时和人工输入监听，调用旧游戏 clearTimers/closePeer/clearNetZones 并移除 iframe；失败时保留冻结的画面供检查，重试再销毁。迟到回调需匹配当前轮次，已取消轮次不能更新新状态。

游戏 iframe 自己的 Storage 方法将 `roomlab-room-progress-v1`、`coop-practice-v1`、`roomlab_records` 写入该 iframe 的内存。正式通关解锁、练习记录和成绩不会写入父页或普通游戏的存储。它不是防恶意脚本的安全沙箱；新增存储键时必须同步检查白名单。其他偏好/资源缓存维持游戏原行为。游戏脚本初次启动可能读取原有进度，但进入测试后的记录写入被隔离。

只读调试接口 `DevConsole.snapshot()` 返回阶段、轮次、配置、房间码、错误、目录以及双方连接/准备/玩法/gameId/tier/数据监听数量/连接监测状态。还返回 bot 的角色、暂停/停止状态、动作数及说明；没有注入答案或通关的接口。部分旧玩法没有公开 gameId，返回 null。旧 catch/beat 加入端保留游戏自身两个 data 监听，不要求所有玩法都是一个；重试必须销毁旧连接。

## 文件及验证

运行文件为 `dev-console.html`、`dev-console.css`、`dev-console.js`、`dev-console-bot.js`；本文为使用说明。没有修改 record、Loading、主界面、地图或玩法模块。

负责人脚本位于 `../qa/tools/qa_dev_console_owner*.cjs`，结果位于 `../qa/reports/evidence/dev-console-owner/`。覆盖全部基础玩法入口、实际难度字段和每个密室的主玩法。原总控另行独立验证双端真实输入同步、键盘焦点、重试换轮、断线恢复、延迟加载取消、进度隔离和桌面布局，证据位于 `../qa/reports/evidence/dev-console-control/`。

这些是本地桌面入口/配置/清理测试，不能替代每个完整谜题的人工体验、手机真机或跨设备弱网发布验收。

## 首批同伴托管（本地待总控验收）

通过“操作方式”选择双端手动、托管 A 或托管 B，可在本局切换。暂停立即取消待执行动作；在托管端点击、按键或触摸会先自动暂停，随后该输入正常交给游戏。人工端输入不会暂停同伴。恢复按钮只恢复当前局，不会续用已取消轮次。

| 玩法 | 托管 A | 托管 B | 协作与限制 |
| --- | --- | --- | --- |
| 密码推条（两档） | 支持 | 支持 | 仅推动自己拥有的行，按可见 SVG 位移跟随人工端第一条。人工仍需调整其他自身行和确认。全行接齐且在合法范围才确认，不读取隐藏密码答案、不消耗机会试错。 |
| 默数开灯（四档） | 支持 | 禁用 | A 通过原开关开始，真实等待 10.5 秒后停止；B 人工收齐全部道具并回出口。B 的可见地图探索尚未可靠适配。 |
| 其余玩法 | 禁用 | 禁用 | 尚未完成可靠适配和完整协作验证，使用双端手动。 |

暂停 A 默数会丢弃机器人自己的计时起点。若恢复时挑战仍在运行，界面说明“起点未知”，保持暂停；先在 A 原游戏开关正常结束当前挑战，再点恢复。不会注入计时重置，也不会补按迟到开关。浏览器长时间阻塞错过 11 秒窗口同样暂停并说明。A 没有 B 的总时限/地图信息，机器人不借用 B 的隐藏数据；B 搜索操作完全保留人工。

重试、返回、页面关闭、玩法结束（包括 win=false）、连接失败都停止旧驱动。连接恢复中先暂停；恢复后由用户明确点恢复。所有正式输入仅走指定端的现有按钮，不修改游戏状态、物品、胜利、地图或录制共享代码。驱动不添加 Peer data 监听。

托管测试：`../qa/tools/qa_dev_console_bot.cjs` 验证独立模块；`../qa/tools/qa_dev_console_bot_integration.cjs` 验证验收台 UI、重试/返回/断线/失败取消。证据在 `../qa/reports/evidence/dev-console-bot/`。测试通过真实本地 Peer 和浏览器控件驱动人工侧，属于自动化模拟人工输入；不冒充真人完整试玩、手机真机或公网弱网验收。第一版 73 个入口检查没有无差别重跑。

## 发布候选兼容小修

运行时检查密码同图切片的 rows/confirmed 结构和切片 DOM；旧 password-strip/1 明确禁用托管。已确认本角色后不再推动；终态 win=true 称成功、win=false 称失败、缺省仅称结束。新密码冻结 JS 1843f006… 的托管 A 入门/B 进阶完整配合及实际机会耗尽失败通过；冻结开灯 E840F6… 真实10.543秒成功。详细准确哈希与证据随 v2 候选快照交付。旧首批冻结快照不覆盖。
