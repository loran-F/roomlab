# 双人搜台

替换旧频段校准，保留 `dial` 标识供地图存档兼容。A 调频率、B 调增益，按住加减、松手停止；键盘左右键也可使用。双方先短练习再准备，3 秒倒计时后限时 100 秒收录三段情报。固定频道 → 缓慢漂移 → 周期干扰。两端同时对准累计 2 秒收录；偏离缓慢回退当前进度，干扰期间冻结进度。爬塔沿用连续同玩法难度对应的限时缩减。

## 固定操作台与外围氛围

显示区、提示条、操作区在两端使用相同尺寸结构，仅仪器与角色颜色不同。准备/结算按钮不改变控制区高度。小屏压缩示波器高度，保留操作按钮。背景、左摆设、右摆设是独立的无交互层，不参与布局、不覆盖操作台。

美术资产就绪后，在 radio-mobile.js 加载前或进入玩法前配置：

```js
window.RadioScenes = {
  'room-4': {
    background: 'assets/scenes/room-4/background.webp',
    left: 'assets/scenes/room-4/lamp.webp',
    right: 'assets/scenes/room-4/toy.webp'
  }
};
```

以上是接入示例路径，当前没有加载不存在的图片；未配置房间使用紫色背景。背景建议竖构图、中心留空，摆设为透明图；左右边缘在手机上可局部裁切。操作台奶油黄、紫色描边，A 蓝 / B 橙，保持前面主界面与手游玩法风格。

## 联机

房主权威计时与模拟。B 仅接收自己的增益、信号质量和公共进度，无目标频率/增益答案。输入带局号和递增序号，180ms 心跳、650ms 过期释放。断线停机，重开需双端同意。大厅和爬塔都进入新模块；爬塔返回沿用原同步返回逻辑。

测试：release-staging/qa_radio.cjs（实际两端按键完成三段、倒计时、练习、私有快照、三种手机尺寸、救场、重开与断线）；qa_radio_dungeon.cjs（爬塔入口、难度限时和 B 返回）。
