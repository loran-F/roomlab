# 新增双人玩法 · 2026-09-08

本模块新增「你别回头」「真假接线员」「影子替身」。独立试玩与密室地图均支持两台设备，通过 PeerJS / WebRTC 共享一局，由房主判定。所有新玩法都有各角色的小练习、双方准备、三秒倒计时、失败原因、断线停止与双方确认重开。

## 你别回头 `lookback`

- 基础限时 85 秒，穿过五段走廊，三次配合机会。
- A 看到前门箭头，告知 B 该开左门还是右门；B 控制门，开对后 A 前进两秒。
- B 看到后方监控：听声怪需要停步，凝视怪需要前进，嗅探怪需要躲藏。A 看不到怪物，必须听 B 指挥。
- 预警 3.2 秒，危险持续 1.8 秒，错误动作累计超过 0.65 秒损失一次机会并重试当前路段。
- B 整局可用一次应急诱饵，吸引当前怪物，给搭档救场。
- A 点选动作后持续执行；切到后台、窗口失焦或输入心跳超过 0.9 秒，会停止移动。键盘 A 使用 ↑ / 空格 / ↓，B 使用 ← / → / 空格。

## 真假接线员 `caller`

- 基础限时 110 秒，处理五通来电，三次误判机会。
- A 看到来电人的姓名和职业，通过按钮询问工号、任务，把回答告诉 B。
- B 看到六人的值班表。表中工号每通随机生成；来电者可能身份完全真实，也可能工号或任务有矛盾。
- 询问两条证据后，双方独立投票放行或拒绝。两票一致才执行；意见不同时可以讨论后改票，不会自动判错。
- 错误决定损失一次机会，并明确指出工号、任务矛盾或误拒真人的原因。
- 这是文字来电原型，不含语音识别或 NPC 语音；玩家通过自己的语音软件交流。

## 影子替身 `shadow`

- 基础限时 100 秒，两轮接力，三次配合机会。第一轮 A 录影 / B 导航，第二轮交换。
- 导航者拿到六个踏板位置，先与录影者商量；双方确认后开始录影。
- 每拍 1.8 秒，录影者选择左 / 中 / 右，到拍点将实际位置录入影子轨迹。
- 三秒交接后自动回放。原录影者拿到出口顺序，报给跑者；跑者逐拍选择出口位置。
- 每个拍点必须同时满足影子踩中踏板、跑者站对出口。失败会说明错在哪一步，扣一次机会后重新规划这一轮。
- 手机点选三个位置；键盘使用 ← / ↓ / →。减少动态效果设置下禁用装饰运动。

## 接入与协作边界

独占文件：`expansion-games.js`、`expansion-games.css`、`assets/map-icons/lookback.svg`、`caller.svg`、`shadow.svg` 和本文档。加载顺序：基础内联脚本结束 → expansion 样式 / 脚本 → coop-upgrade 样式 / 脚本 → `render()`。

不修改原玩法引擎。包装链转发旧玩法的原函数；自动注册三条 PLAYGROUNDS 和 EV_POOL_NET。地图入口继续由原 `dgEnterHost` 锁分支和广播，再经过 `renderPlayDungeonHost` / `renderManual` 进入新玩法。与现有优化模块共用 `coopTime`，连续同玩法时按完成路径缩短限时。

独立协议：`xWelcome`、`xState`、`xInput`、`xRestart`。输入校验局号、模式、递增序号、轮次、阶段与角色。快照只发该角色所需字段；同伴无法从快照直接读取另一方的怪物 / 门向、来电 / 值班表或隐藏路线。退出后不接收其他模式的残留快照。

## 验证

原理测试位于项目父目录 `release-staging/expansion-core-test.cjs`：三个通关路径、超时、私密快照、未询问就投票、分歧改票、一次性诱饵、影子换职、过期输入。浏览器联测脚本及截图分别位于 `release-staging/expansion-browser-test.cjs` 和 `release-staging/expansion-qa`。

已通过真实 A/B 浏览器按钮操作完成三种玩法的完整通关、双方确认重开、断线停止；320×568、390×844、720×1280 无横向溢出。合并后的地图联测（`expansion-dungeon-test.cjs`）通过三个节点实际进入、合作准备、重复玩法限时递减、B 主动返回不扣血、退出后残留快照忽略、超时失败恰好扣一次共享生命。全部测试场景无页面脚本异常。地图失败验证直接缩短了剩余时间以触发超时，独立通关使用双方界面显示的线索驱动按钮，未直接设置胜利。

使用桌面 Edge 的手机视口与实际 WebRTC 两端连接验证；未代替实体手机、跨地域网络和真人趣味性测试。
