# 默数开灯（lightsearch）

状态：本地开发与验证完成，尚未提交、上传或发布。共享页面的接入由任务（2）完成，本模块没有修改 HTML、APP_VER 或爬塔抽取池。

## 文件与入口

- `rhythm-light.js`：自注册 PLAYGROUNDS、包装 render / clearTimers，独立建房、握手、权威逻辑、角色视图与输入清理。
- `rhythm-light.css`：冷色角色界面、灯绳插画、房间地图和单层按钮。B 行动阶段固定视口。
- 加载顺序：既有模块 → password-strip.css/js → rhythm-light.css/js → 最终 render()。
- 入口：`roomlab.html?m=lightsearch`；指定角色可用 `&role=A` 或 `&role=B`。首版只在独立玩法大厅，不进爬塔。

## 规则

双方准备后，A 得到一次起拍。A 在心里默数约 10 秒再拉灯，首版正确窗口为起拍或上次操作后的 8.5–11.5 秒。成功时全场亮 10 秒，并以上次拉动重新默数；太早、太晚时立即变暗，给一次明确的新起拍。

漏过窗口时，系统变暗并给一次新的起拍；之后不会循环自动发起拍。如果继续不操作，A 不会得到每 10 秒的提醒。A 可按新的起拍默数恢复，或再操作后按反馈重新起拍。界面不显示动态倒计时、节拍进度或灯光剩余时长。

B 在 9×11 网格房间内移动。书柜、沙发和工作台构成地标与障碍；起点固定，总闸在三个可达位置中随机选一处。亮时可看全景，暗时只能看到身边约 1.7 格的小光圈，可继续移动。按住方向键连续走动，也可点击走一格。靠近物件（曼哈顿距离≤1）才能查看；靠近配电箱后实际点击“扳下总开关”或地图中配电箱，双方胜利。没有随时可点击的屏幕外成功按钮。

箱子和绿植可查看但不是强制收集，不影响通关。首版无总限时、血量或多关机制。

## 同步与输入边界

- 独立 Peer ID 前缀 `lightsearch-`，连接 metadata 标识 mode/protocol；`lsHello` / `lsWelcome` 握手完成才进入玩法。错码、其他玩法或版本错配有明确提示。
- `lsAction`：包含局 id、单调 seq、action/value。房主按真实来源分配角色：A 仅 ready/pull/again；B 仅 ready/move/interact/again。不信任包内角色或坐标。
- 局 id、重复/过旧序号、异常序号跃升、方向枚举和角色权限检查；B 不能发拉灯、传坐标瞬移或远程开闸。移动由房主检查可行走格。
- B 持续移动每 180ms 发保活，650ms 无输入自动释放；pointerup/cancel/lostcapture、键盘松开、失焦和页面隐藏均释放。
- A 快照没有地图、玩家位置或总闸位置。B 快照没有起拍时间、剩余秒数或节奏判定；暗时只发附近可见格、物件与相交家具，画面再做圆形裁切。
- `lsState` 含轮次和修订号，客户端拒绝过旧局/重复快照；胜利后必须双方同意才重开。
- 退出发送 `lsBye`，断线停止计时与操作，不在失联状态继续推进。
- 权威时间来自房主 `performance.now()`；没有缩放测试时间混入产品逻辑。

## 验证记录

测试位于 `release-staging/lightsearch/`。

1. `model.cjs`：纯模型的显式时间参数测试，不属于真实等待。验证 10 秒亮灯、早拉、错过窗口单次起拍、恢复、可见信息、无效/重复/旧局/越权/非法方向输入、碰撞、输入租约及三个随机目标可达。
2. `live.cjs`：两台实际浏览器与 WebRTC，真实等待约 10 秒，成功亮灯实测 **9999ms**；随后提前拉错、真实等待恢复。B 用实际鼠标/键盘操作到配电箱，按邻近交互通关；路径由程序从 B 的亮灯快照计算，无坐标或玩家状态注入。还验证键盘在按钮获焦后可用、失焦释放、异常输入、双方重开、旧快照拒绝及断线停止。日志 `live-trace.json`。
3. `missed.cjs`：真实等待漏过窗口，再等待超过 12 秒，确认没有周期性起拍；主动晚拉后重新默数成功。验证 320×568、390×844、720×1280 下 B 地图和五个操作按钮都在视口内，每个按钮≥48px，地图每格≥22px。日志 `missed-trace.json`。
4. `rooms.cjs`：其他玩法房间码无法误入、错误 metadata 被拒绝且不占据房主、随后正常入房、错误握手版本被拒绝、主动退出停止同伴。

截图：`A-320/390/720.png`、`A-latest.png`；B 最新适配图为 `B-dark-fit-320/390/720.png`、`B-light-fit-320/390/720.png`。较早 `B-dark.png`、`B-320/390/720.png` 是适配前截图，不能作为最终布局依据。

## 首版限制

- 房间布局固定，配电箱三选一；熟悉布局后搜索会更容易，尚未引入程序化房间或递增难度。
- 网络判定以房主接收时刻为准；±1.5 秒容差用于首版，未引入时钟同步或延迟补偿。
- B 在暗处也可以凭记忆移动并开闸，不强制必须成功开灯后才允许通关。
- 没有每秒声音或闪烁提示；A 应自行默数。玩法准备页保留规则，B 活动阶段收起说明以保证操作区常驻。

## 2026-09-08 密室适配（本地，待总对话统一发布）

- 注册 `RoomGameAdapters.lightsearch.host(ctx)/guest(ctx)`，遵循 `ROOM_GAME_ADAPTERS.md` v1；复用现有 `PEER.conn`，不建第二房、不叠加通用练习。
- 包装 `dgHostOnData/dgGuestOnData`，非 ls 消息透传。密室不安装额外 data listener；`lsAction/lsState` 带 `sessionId`，并保留局 id、round、revision、seq 校验。
- 主机成功只调用一次捕获的 `ctx.finish(true)`，B 等地图结算；退出 `ctx.abort()`。clearTimers 清理本模块的 interval、键盘、指针状态、连接 close listener，保留地图连接。断线后允许返回密室选择。
- room-2 显示幽宅灯控：冷紫家具墙面、幽灵轮廓、角色主题提示。theme 由主机随快照同步，其他密室及独立入口不启用。
- 密室 tier 1/2/3/4 的成功误差为 ±1500/1200/900/700 ms；仍默数10秒、亮10秒、不显示倒计时，独立入口保持原有容错。
- 实际双页 WebRTC 验证：地图进入、双方准备、真实9.75秒拉灯、方向按钮到达总开关、双方回图；下一节点旧包拒绝；B退出无扣血；断线停局并返回选择。见 `../release-staging/lightsearch/dungeon.cjs` / `dungeon-trace.json`。
- 纯模型计时/私密性/进阶容错通过；独立 `live.cjs` 实际10秒亮灯、提前恢复、键盘、失焦、重开及断线回归通过。
