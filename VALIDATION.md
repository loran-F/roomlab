# v10.1 卷轴地图验证

2026-09-07，两个独立 Edge 页面通过实际 PeerJS / WebRTC 联测通过：

- 14 个 SVG 返回正常并成功解码显示。
- 进入地图播放展开动画；减少动态效果时直接展开。
- 320×568、390×844、720×1280 均无横向溢出，节点点击区域 52px，均位于地图内。
- A/B 点击移动与位置同步正常；快照更新复用卷轴外壳，不重复播放动画。
- 集合倒计时进度条连续，进入玩法后可从 B 端返回地图，两端生命保持正常。
- 验证期间无页面 JavaScript 异常。

以下为此前玩法版本的验证记录。

# v10 验证记录

验证日期：2026-09-07。使用本地静态服务与 PeerJS 信令，以及两个独立 Edge 浏览器上下文进行实际 WebRTC 联测。自动化浏览器关闭 mDNS 隐藏本机地址的特性，以便同机测试；游戏代码未改动浏览器或系统网络设置。

## 已通过

| 范围 | 验证内容 |
|---|---|
| 选择界面独立发布包 | 保留 v9 与原有 11 玩法，不含新增 Boss；13 封面正常、循环翻页、触摸滑动、刷新记忆、A/B 建房与密室选择同步 |
| Boss 独立试玩 | 3 秒倒计时；通过操作按钮完成三个阶段；两端胜利；双方确认重开；两端失败 |
| Boss 密室入口 | 完成前置节点后，通过实际地图点击和 3 秒同点读条进入 |
| Boss 密室结算 | 两端整局胜利，Boss 节点完成；最后 1 格生命下超时，两端归零失败；房主重开同步给 B |
| Boss 边界 | 提前撤离按键和超过 0.38 秒间隔按键判错；退出回地图；连接关闭后停止判定并禁用按钮 |
| 档案难度 | 三个事件层分别为 5 / 6 / 7 位、1 / 0.8 / 0.6 秒；A/B 配置一致；独立试玩保持 5 位 |
| 档案录入 | 错码扣时并清空 B 记录；按位序输入正确密码后两端返回地图；倒计时中退出后不会继续创建旧玩法 |
| 动力稳压 | 使用 A 加减压与 B 开阀完成三轮，两端返回地图；两端快照保持信息不对称 |
| 动力稳压失败 | A 持续加压爆表后，两端密室生命只扣 1 格；模型校验区间外硬开阀会损坏设备 |
| 手机尺寸 | 320×568、390×844、720×1280 下新实时界面与选择页无横向溢出；小屏数字键盘位于面板内 |
| 基础检查 | JavaScript 语法检查；12 玩法注册、11 普通事件，压力已入池且 Boss 不在普通事件池；验证场景无页面 JavaScript 异常 |

Boss 通关测试根据画面提示驱动双方按钮，没有直接设置胜利或增加进度。地图前置节点、指定事件与最后一格生命的超时属于定点测试场景设置。

## 尚需真人体验

本次使用浏览器手机尺寸模拟，未进行实体手机、跨地域网络或真人配合趣味性测试。公共信令的可达性仍取决于网络环境。13 张封面目前共用玩法规则，展示评分、时长和难度为原型配置。

## 发布顺序

1. 选择界面独立版本（v9 基线 + 密室选择页）。
2. 特工行动完整版本（v10，包含 A–D）。

本文件记录本地验证，不代表 GitHub 已提交或 Pages 已更新。发布须分别验证 API 返回的文件 SHA 和 Pages 实际文件内容。
